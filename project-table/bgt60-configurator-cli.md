# bgt60-configurator-cli/
> Generated by /map. Do not edit manually — re-run /map to refresh.

- **bgt60-configurator-cli.exe** — Vendor-provided command-line tool (per `README.txt`: `bgt60-configurator-cli -c path/to/config.json`) that converts a JSON radar configuration into a register-value list writable to a BGT60TRxx sensor.
- **lib_avian.dll, lib_smartar.dll, sdk_algo.dll, sdk_avian.dll, sdk_base.dll, sdk_fmcw.dll, sdk_radar.dll, sdk_radar_device_common.dll, strata_shared.dll** — Infineon Radar SDK shared libraries required at runtime by the CLI executable; not application source.
- **config_1400mhz.json** — Active chirp config: `lower_frequency_Hz=60550000000`, `upper_frequency_Hz=61950000000` (1.4 GHz span), `rx_antennas=[2,3]`, `tx_antennas=[1]`, `num_chirps_per_frame=32`, `num_samples_per_chirp=128`, `chirp_repetition_time_s=5e-4`, `frame_repetition_time_s=150e-3`, `sample_rate_Hz=2000000`. Matches `src/presence_radar_settings.h` exactly.
- **config_2ghz.json** — Prior chirp config: `lower_frequency_Hz=60250000000`, `upper_frequency_Hz=62250000000` (2.0 GHz span), otherwise identical parameters to `config_1400mhz.json`. Superseded by the 1.4 GHz config in the active firmware.
- **config.json** — A third, narrower-band config: `lower_frequency_Hz=61020098000`, `upper_frequency_Hz=61479902000` (~0.46 GHz span). Matches the frequency constants used in `data_test/src/radar_process_test.py`, suggesting it corresponds to an earlier/narrower capture used for that analysis script.
- **README.txt** — Vendor usage documentation for the CLI tool.

**Responsibility:** offline generation of the BGT60TR13C register set from human-tunable JSON chirp parameters.
**Relationships:** output values are manually copied (with diff annotations) into `src/presence_radar_settings.h` and `templates/TEST-000/presence_radar_settings.h`; no automated build-time dependency — regeneration is a manual step.
