# bsps/
> Generated by /map. Do not edit manually — re-run /map to refresh.

### TARGET_APP_CY8CPROTO-062S3-4343W/
- **Responsibility:** ModusToolbox-generated Board Support Package for the CY8CPROTO-062S3-4343W target (device CY8C6245LQI-S3D72), providing dual-core startup, pin/clock/peripheral configuration, and linker scripts.
- **Key files:** `cybsp.c`/`cybsp.h`/`cybsp_hw_config.h`/`cybsp_types.h` (board init API), `bsp.mk` (build glue), `config/design.modus` + `config/GeneratedSource/cycfg*.c/h` (Device Configurator output: clocks, pins, peripherals, system, QSPI, CAPSENSE), `COMPONENT_CM4/`+`COMPONENT_CM0P/` (per-core `system_psoc6_*.c` and `TOOLCHAIN_{GCC_ARM,ARM,IAR,A_Clang}/` startup assembly + linker scripts), `bluetooth/cybsp_bt_config.c/h`, `deps/*.mtbx` (BSP-level dependency pins on cat1cm0p/core-lib/core-make/mtb-hal-cat1), `docs/` (HTML API reference), `cyreservedresources.p2/p9/p12`.
- **Relationships:** selected by the top-level `Makefile` via `TARGET=APP_CY8CPROTO-062S3-4343W`; supplies the pin macros (`Radar_CS_1`, `Radar_SPI_CLK`, `P7_0`/`P7_1`, `P10_3`, etc.) and `cybsp_init()`/`cy_retarget_io_init()` entry points consumed by `src/main.c` and `src/radar_task.c`; declares its own dependency versions against `libs/`.
