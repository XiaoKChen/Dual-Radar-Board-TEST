# bsps/
> Generated by /map. Do not edit manually — re-run /map to refresh.

Owns the hardware abstraction layer for the CY8CPROTO-062S3-4343W target: dual-core startup code, linker scripts for CM0+ and CM4, BSP HAL init (`cybsp_init`), and board-level pin/type definitions.

- **Responsibility:** Board bring-up — startup assembly, linker scripts, `cybsp_init`, pin aliases, BT config stubs
- **Key files:** `cybsp.c`, `cybsp.h`, `cybsp_hw_config.h`, `cybsp_types.h`, `bsp.mk`, `COMPONENT_CM4/system_psoc6_cm4.c`, `COMPONENT_CM0P/system_psoc6_cm0plus.c`
- **Relationships:** consumed by `src/main.c` via `#include "cybsp.h"`; provides `cybsp_init()`, pin macro definitions (`Radar_CS_1`, `Radar_SPI_CLK`, etc.) referenced in `src/radar_task.c`

---

## TARGET_APP_CY8CPROTO-062S3-4343W/

- **`cybsp.h`** — BSP top-level header: exposes `cybsp_init()`, includes `cybsp_types.h` and `cybsp_hw_config.h`, conditionally includes BT config for WICED builds.

- **`cybsp.c`** — Implements `cybsp_init()`: configures system clocks, power management callbacks, and calls HAL init.

- **`cybsp_hw_config.h`** — Hardware defaults header: sets fallback defines for `CYBSP_USER_BTN_DRIVE`, `CYBSP_DEBUG_UART_CTS/RTS`, includes `cycfg_pins.h` which provides the radar-specific pin aliases (`Radar_CS_1`, `Radar_CS_2`, `Radar_IRQ_1`, `Radar_IRQ_2`, `Radar_SPI_CLK`, `Radar_SPI_MOSI`, `Radar_SPI_MISO`, `Radar_RST`) referenced in `src/radar_task.c`.

- **`cybsp_types.h`** — Board-level type and pin-state macros: LED active-low (`CYBSP_LED_STATE_ON = 0`), Wi-Fi interface type selection macros.

- **`cybsp_doc.h`** — Doxygen documentation stubs only; no runtime content.

- **`bsp.mk`** — BSP makefile fragment: declares target device (`CY8C6245LQI-S3D72`), selects `CM0P_SLEEP` BSP component, adds `CY_USING_HAL` and XIP defines, and wires the `recipe-make-cat1a` recipe.

- **`system_psoc6.h`** — Top-level system header shared by CM0+ and CM4 system files; declares `SystemInit`, `SystemCoreClockUpdate`.

### COMPONENT_CM4/

- **Responsibility:** CM4 core startup and system initialization — startup assembly, linker script, `SystemInit`
- **Key files:** `system_psoc6_cm4.c`, `TOOLCHAIN_GCC_ARM/startup_psoc6_03_cm4.S`, `TOOLCHAIN_GCC_ARM/linker.ld`
- **Relationships:** runs after CM0+ releases CM4 from reset; initializes IPC pipes for inter-core communication

- **`system_psoc6_cm4.c`** — CM4 system initialization: sets up SystemCoreClock, IPC semaphores and pipes for inter-core communication (CM0+↔CM4), and watchdog configuration at startup before `main()`.

- **`TOOLCHAIN_GCC_ARM/startup_psoc6_03_cm4.S`** — CM4 GCC startup assembly: sets up stack/heap, zeroes BSS, copies `.data`, calls `SystemInit` and `main`.

- **`TOOLCHAIN_GCC_ARM/linker.ld`** — GCC linker script for CM4: partitions flash (`.text`, `.rodata`), SRAM (`.data`, `.bss`, heap, stack), and QSPI XIP regions.

### COMPONENT_CM0P/

- **Responsibility:** CM0+ core startup — runs first at boot, initializes IPC infrastructure, releases CM4 from reset
- **Key files:** `system_psoc6_cm0plus.c`, `TOOLCHAIN_GCC_ARM/startup_psoc6_03_cm0plus.S`, `TOOLCHAIN_GCC_ARM/linker.ld`
- **Relationships:** executes before CM4; in production this core is replaced by the pre-built `cat1cm0p` sleep image from `libs/`

- **`system_psoc6_cm0plus.c`** — CM0+ system initialization: parallel structure to the CM4 file; runs first at boot, initializes IPC infrastructure, then releases CM4 from reset.

- **`TOOLCHAIN_GCC_ARM/startup_psoc6_03_cm0plus.S`** — CM0+ GCC startup assembly.

- **`TOOLCHAIN_GCC_ARM/linker.ld`** — GCC linker script for CM0+.

### bluetooth/

- **`cybsp_bt_config.c` / `cybsp_bt_config.h`** — BT pin and UART configuration stubs for the CYW4343W; unused in this radar application but required by the BSP library.

### config/

- **`design.modus`** — ModusToolbox Device Configurator project file: defines all peripheral assignments, pin muxing, and clock configuration; consumed by code-generation tools (not directly compiled).

- **`design.cycapsense`** / **`design.cyqspi`** — CapSense and QSPI configurator project files; not used by this application.

### deps/

- BSP-level `.mtbx` dependency locks (cat1cm0p, core-lib, core-make, mtb-hal-cat1, mtb-pdl-cat1, recipe-make-cat1a); managed by ModusToolbox, not edited directly.
