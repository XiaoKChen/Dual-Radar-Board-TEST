# imports/
> Generated by /map. Do not edit manually — re-run /map to refresh.

- **emusb-device/Config/COMPONENT_CAT1A/usbd_config.c** — CAT1A-specific emUSB-Device stack configuration (device descriptors, endpoint config) for the USB CDC implementation that has since been removed from the active build.
- **emusb-device/Config/COMPONENT_CAT3/usbd_config.c** — Equivalent emUSB-Device configuration for CAT3-family devices.
- **emusb-device/Config/usbd_config_io.c** — Shared USB I/O configuration glue for the emUSB-Device stack.
- **emusb-device.cksum** — Checksum file for verifying the imported emusb-device asset bundle's integrity.

**Responsibility:** preserve the now-unused USB CDC device-stack configuration from before the firmware switched to plain UART-based logging (`src/usb_logging.c`).
**Relationships:** excluded from the build by both `.cyignore` (`imports/emusb-device`) and the `Makefile`'s `CY_IGNORE+=../mtb_shared/emusb-device`; no active source file in `src/` references emUSB-Device APIs.
