# templates/
> Generated by /map. Do not edit manually — re-run /map to refresh.

### Per-board `design.modus` template directories (16 total)
- **Responsibility:** archived Device Configurator pin/clock/peripheral definitions for alternative PSoC 6 target boards not currently built (`TARGET_CY8CEVAL-062S2*` ×6, `TARGET_CY8CKIT-062S2-43012`, `TARGET_CY8CKIT-062S4`, `TARGET_CY8CKIT-062-WIFI-BT`, `TARGET_CY8CKIT-064B0S2-4343W`, `TARGET_CY8CPROTO-062-4343W`, `TARGET_CY8CPROTO-062S2-43439`, `TARGET_CY8CPROTO-062S3-4343W`, `TARGET_CY8CPROTO-064B0S3`, `TARGET_CYW9P62S1-43012EVB-01`, `TARGET_CYW9P62S1-43438EVB-01`).
- **Key files:** each directory contains exactly one file, `config/design.modus`.
- **Relationships:** excluded from the active build by `.cyignore` (`templates`); the `TARGET_CY8CPROTO-062S3-4343W` template here is a static config artifact separate from the actively-used, richer `bsps/TARGET_APP_CY8CPROTO-062S3-4343W/` BSP.

### TEST-000/
- **Responsibility:** archived single-radar (not dual-radar) baseline firmware demonstrating raw FIFO streaming without the presence-detection pipeline.
- **Key files:** `main.c`, `presence_radar_settings.h`.
- **main.c** — Single-sensor bring-up app: initializes one `xensiv_bgt60trxx_mtb_t` sensor over SPI at 25 MHz, enables an LDO pin, and implements a `start [N]`/`stop` CLI over the debug UART. On each FIFO-full interrupt it reads `NUM_SAMPLES_PER_FRAME` raw `uint16_t` samples and streams them via `send_frame_binary()`, which writes a packed `binary_frame_header_t` (`magic="RADR"`, `version=1`, `sample_size_bytes=2`, `frame_index`, `sample_count`) followed by the raw sample array — the exact format consumed by `data_test/src/serial_logger.py` and `format_binary_frames.py`. No FFT, no presence detection, no FreeRTOS — bare superloop.
- **presence_radar_settings.h** — A different chirp config than the active `src/` one: `START_FREQ_HZ=60999999000`, `END_FREQ_HZ=62200001000`, `NUM_CHIRPS_PER_FRAME=64`, `NUM_RX_ANTENNAS=3` (vs. current 2), `SAMPLE_RATE=2424242`, `FRAME_REPETITION_TIME_S=0.0999474`; `NUM_REGS=38`.
- **Relationships:** excluded from the active build by `.cyignore`; its wire format is the one `data_test/`'s capture scripts actually implement.
