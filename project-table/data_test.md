# data_test/
> Generated by /map. Do not edit manually — re-run /map to refresh.

- **src/serial_logger.py** — CLI tool (`argparse`) that opens a serial port (`pyserial`), sends a `start`/`start N` command, aligns on a 16-byte `"RADR"` header (`struct.Struct("<4sHHII")`: magic, version, sample_size_bytes, frame_index, sample_count), and writes raw header+payload bytes to an output file; optionally also writes a human-readable per-chirp/per-sample dump (`--rx-antennas` default 3, `--samples-per-chirp` default 128) via `_write_formatted_frame`. Sends `stop` on exit if `--stop-on-exit`. Targets the legacy single-radar streaming protocol from `templates/TEST-000/main.c`, not the current V3 protocol.
- **src/format_binary_frames.py** — Offline decoder for the same `"RADR"`-header capture format (`HEADER_STRUCT`, `HEADER_MAGIC=b"RADR"`, `SUPPORTED_VERSION=1`). `decode_frames()` iterates frames (`_iter_frames`), unpacks samples per `SUPPORTED_SAMPLE_SIZES={1:"B",2:"H",4:"I"}`, and can emit a human-readable text dump and/or a CSV (`frame,chirp,sample,rx,value`) via `_write_frame_text`/`_maybe_write_csv`. CLI defaults `--rx-antennas 3`, `--samples-per-chirp 128`, matching `templates/TEST-000`.
- **src/radar_process.py** — Script-style (with `# %%` cell markers) NumPy/SciPy/Matplotlib pipeline: parses `frames.txt` (`parse_radar_log`, 3-column bracketed samples) into a `(Nchirp, Nsample, Nrx)` cube, supports a synthetic-target `'sim'` mode or `'real'` mode (Butterworth lowpass filter, DC removal, Hilbert transform), computes a Range FFT (peak detection via `scipy.signal.find_peaks`), a Hanning-windowed Doppler FFT, and a digital-beamforming (DBF) AoA scan (±40°, 0.2° steps) using a 2-element steering-vector formulation; plots Range profile, Doppler spectrum, Range-Doppler maps per antenna, and DBF beampattern. Radar constants (`fc_start=60550001000`, `fc_end=61950001000`) match the current 1.4 GHz config.
- **src/radar_process_test.py** — Similar Range/Doppler/AoA pipeline but reads `frames.txt` via its own `parse_frames_txt` (frame-boundary aware), applies a Butterworth lowpass + DC removal, an exponential "Smart MTI" IIR clutter filter (`alpha=0.1`), a Hanning-windowed Doppler FFT with `fftshift`, sums power across antennas, and animates a live Range-Doppler map with `detect_peaks` (local-maxima filter, configurable dB threshold). Uses `fc_start=61020099000`, `fc_end=61479903000` — matching `bgt60-configurator-cli/config.json`, a narrower/different band than the current firmware config.
- **src/radar_process_test_001.py** — A from-scratch Python reimplementation of `src/presence_detection.c`'s state machine (`RadarPresenceDetector` class), intended as a bit-accurate offline model: `FIRFilter` class emulates `arm_fir_f32` per-sample block-size-1 filtering; constants (`START_FREQ_HZ=60550001000`, `END_FREQ_HZ=61950001000`, `NUM_SAMPLES_PER_CHIRP=128`, `NUM_CHIRPS_PER_FRAME=32`) mirror `src/presence_radar_settings.h`; `CONFIG` dict documents user-tuned overrides differing from firmware defaults (`macro_threshold=2.0`, `micro_threshold=20.0`, `max_range_bin=15`, bandpass explicitly enabled here even though disabled in firmware); implements the same macro/micro-detection state machine as `presence_process_frame`, plus `process_next_frame`/`run_simulation_demo`/`run_realtime_demo` helpers for synthetic and live-clock testing.
- **src/frames.txt** — Captured raw radar text log (4162 lines) in the "Frame N / Chirp NNN / Sample NNN: [v1, v2, v3]" format produced by the legacy 3-RX-antenna capture path (`serial_logger.py`/`templates/TEST-000/main.c`); used as the fixture input by all three `radar_process*.py` scripts.
- **main.py** — Trivial placeholder entry point (`print("Hello from data-test!")`), unrelated to the radar processing scripts in `src/`.
- **pyproject.toml** — `uv`/PEP 621-style project manifest, `name="data-test"`, `requires-python>=3.13`, dependencies: `matplotlib>=3.10.7`, `numpy>=2.3.5`, `pyserial>=3.5`, `scipy>=1.16.3`.
- **README.md** — Present but effectively empty.

**Responsibility:** host-side capture, decoding, and offline algorithmic validation of radar data, spanning both a legacy 3-RX raw-sample protocol and independent reimplementations of the current presence-detection algorithm.
**Relationships:** `radar_process*.py` scripts duplicate logic and constants from `src/presence_detection.c`/`src/presence_radar_settings.h` by hand (no shared code); `serial_logger.py`/`format_binary_frames.py` target the protocol from `templates/TEST-000/main.c`, an older single-radar firmware variant, not the current dual-radar V3 UART protocol.
