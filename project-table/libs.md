# libs/
> Generated by /map. Do not edit manually — re-run /map to refresh.

- **abstraction-rtos.mtb** — `release-v1.12.0`, RTOS abstraction layer used by HAL.
- **cat1cm0p.mtb** — `release-v1.9.0`, CM0+ core support library for CAT1 devices.
- **clib-support.mtb** — `release-v1.8.0`, newlib/toolchain C-library support glue.
- **cmsis.mtb** — `release-v5.8.2`, ARM CMSIS headers/DSP library (source of `arm_rfft_fast_f32`, `arm_cfft_f32`, etc. used in `presence_detection.c`).
- **core-lib.mtb** — `release-v1.6.0`, Cypress/Infineon core utility library.
- **core-make.mtb** — `release-v3.8.0`, the ModusToolbox core make-system engine.
- **device-db.mtb** — `release-v4.33.0` (from `$$GLOBAL$$`), device/SVD database used for register-map resolution (`DEVICE_CY8C6245LQI-S3D72_SVD` in `mtb.mk`).
- **mtb-hal-cat1.mtb** — `release-v2.7.4`, the HAL implementation for CAT1 (PSoC6) — source of `cyhal_spi_*`, `cyhal_gpio_*` used in `src/radar_task.c`.
- **mtb-pdl-cat1.mtb** — `release-v3.18.0`, the Peripheral Driver Library for CAT1.
- **recipe-make-cat1a.mtb** — `release-v2.6.0`, the CAT1A build-recipe makefiles (compiler flags, memory maps).
- **app.mk** — Generated makefile dispatching GUI configurator targets (`config`, `bsp-assistant`, `library-manager`, `capsense-configurator`, etc.) to `make -C Dual-Radar-Board-TEST $@`.
- **mtb.mk** — Generated makefile defining `SEARCH_*` paths for all 11 resolved libraries plus the BSP, `COMPONENTS += MW_*` flags, the device SVD path, and make targets for every ModusToolbox configurator GUI.

**Responsibility:** hold the full resolved dependency closure (11 `.mtb` pins) and the generated make-system glue (`app.mk`, `mtb.mk`) that wires them into the GCC_ARM build for `TARGET=APP_CY8CPROTO-062S3-4343W`.
**Relationships:** generated from `deps/` + BSP requirements by `make getlibs`; included transitively by the top-level `Makefile`.
