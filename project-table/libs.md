# libs/
> Generated by /map. Do not edit manually — re-run /map to refresh.

Holds resolved `.mtb` references for the full library closure (HAL, PDL, CMSIS, FreeRTOS abstraction, emUSB, core-make, device-db) and generated make-system glue (`app.mk`, `mtb.mk`).

- **Responsibility:** Resolved library closure — all vendored library pointers and makefile integration
- **Key files:** `app.mk`, `mtb.mk`, `abstraction-rtos.mtb`, `mtb-hal-cat1.mtb`, `mtb-pdl-cat1.mtb`, `cmsis.mtb`, `core-lib.mtb`, `core-make.mtb`, `recipe-make-cat1a.mtb`, `device-db.mtb`, `clib-support.mtb`, `cat1cm0p.mtb`
- **Relationships:** referenced by `Makefile` via ModusToolbox core-make; `.mtb` files are resolved by `make getlibs` into the shared asset repo; supplies headers and sources consumed by `src/`

---

- **`app.mk`** — Generated makefile forwarding IDE tool invocations (`device-configurator`, `library-manager`, etc.) to the project sub-make; regenerated on every `make getlibs` run.

- **`mtb.mk`** — ModusToolbox generated makefile including all resolved library paths into the build.

- **`abstraction-rtos.mtb`** — Reference to the RTOS abstraction layer (wraps FreeRTOS APIs in a common interface used by Infineon middleware).

- **`mtb-hal-cat1.mtb`** — Reference to the CAT1 Hardware Abstraction Layer (`cyhal_*` APIs used throughout `src/`).

- **`mtb-pdl-cat1.mtb`** — Reference to the CAT1 Peripheral Driver Library (`Cy_GPIO_*`, `cy_pdl.h` used in `src/radar_task.c`).

- **`cmsis.mtb`** — Reference to CMSIS-DSP/Core headers (`arm_math.h`, FFT instances used in `src/presence_detection.c`).

- **`core-lib.mtb`** — Reference to Infineon core utility library (`cy_result.h`, `cy_utils.h`).

- **`core-make.mtb`** — Reference to the ModusToolbox core build system recipes.

- **`recipe-make-cat1a.mtb`** — Reference to the CAT1A (PSoC 6) specific build recipe.

- **`device-db.mtb`** — Reference to the device database (provides device-specific linker/startup metadata).

- **`clib-support.mtb`** — Reference to C library support shims (newlib/ARM C library integration for the embedded toolchain).

- **`cat1cm0p.mtb`** — Reference to the pre-built CM0+ sleep image deployed to the CM0+ core at reset.
