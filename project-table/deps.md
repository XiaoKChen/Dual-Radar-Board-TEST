# deps/
> Generated by /map. Do not edit manually — re-run /map to refresh.

Declares external library dependencies as ModusToolbox `.mtb` URL-pinned manifests; resolved by `make getlibs` into the shared asset repo.

- **Responsibility:** Dependency declaration — versioned Git references for all third-party libraries
- **Key files:** `sensor-xensiv-bgt60trxx.mtb`, `freertos.mtb`, `emusb-device.mtb`, `retarget-io.mtb`, `assetlocks.json`
- **Relationships:** consumed by the ModusToolbox build system (`make getlibs`); feeds resolved libraries into `libs/`

---

- **`sensor-xensiv-bgt60trxx.mtb`** — Points to Infineon `sensor-xensiv-bgt60trxx` at `release-v2.0.0`; provides `xensiv_bgt60trxx_mtb.h` and the MTB HAL integration layer used in `src/radar_task.c`.

- **`freertos.mtb`** — Points to `freertos` at `release-v10.6.202`; supplies the FreeRTOS kernel consumed by `src/FreeRTOSConfig.h`.

- **`emusb-device.mtb`** — Points to `emusb-device` at `release-v1.4.0`; supplies `USB.h` and `USB_CDC.h` used in `src/usb_logging.c`.

- **`retarget-io.mtb`** — Points to the retarget-io library; supplies `cy_retarget_io_init` used in `src/main.c` for UART debug output via `printf`.

- **`assetlocks.json`** — Records exact resolved commit SHAs for all transitive dependencies; updated by ModusToolbox to lock the dependency graph.
