# deps/
> Generated by /map. Do not edit manually — re-run /map to refresh.

- **freertos.mtb** — Pins FreeRTOS to `release-v10.6.202` from `cypresssemiconductorco/freertos`, resolved into `$$ASSET_REPO$$/freertos/release-v10.6.202`.
- **retarget-io.mtb** — Pins `retarget-io` to `release-v1.8.1`, the library backing `cy_retarget_io_init()`/`printf`-over-UART used throughout `src/`.
- **sensor-xensiv-bgt60trxx.mtb** — Pins the Infineon BGT60TRxx sensor driver to `release-v2.0.0`, providing `xensiv_bgt60trxx_init/config/start_frame/get_fifo_data` used in `src/radar_task.c`.
- **assetlocks.json** — JSON array locking exact commits (`release-v*`) for all 10 resolved libraries in `libs/` plus the app-level deps above; ensures reproducible `make getlibs` resolution.

**Responsibility:** declare and version-lock the three direct middleware dependencies this application requires.
**Relationships:** consumed by ModusToolbox's `make getlibs`, which resolves these into the shared `mtb_shared/` tree referenced by `libs/mtb.mk`.
