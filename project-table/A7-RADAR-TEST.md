# A7-RADAR-TEST/
> Generated by /map. Do not edit manually — re-run /map to refresh.

- **src/main.cpp** — Single-file Arduino sketch implementing the UART-to-USB bridge. Defines protocol constants matching `src/radar_protocol.h` (`SYNC0/SYNC1`, `TYPE_V3=0x03`, `PAYLOAD_LEN=14`, `FRAME_LEN=20`, `REC_SIZE=6`). A 7-state `RxState` enum (`WAIT_SYNC0`…`READ_CRC1`) drives `handle_byte()`, a byte-at-a-time FSM that reconstructs frames into `rx_frame[FRAME_LEN]`, validates the declared length against `PAYLOAD_LEN` and the CRC (`crc16_ccitt_false`, identical algorithm to the firmware) before emitting JSON via `emit_frame_json()`/`emit_record_json()`. `apply_command()` implements a `normal`/`binary`/`stat` command interface read from USB `Serial` by `poll_usb_commands()`. `poll_link()` reads `Serial1` and either passes bytes through (`MODE_NORMAL`) or feeds the FSM (`MODE_BINARY`, default). `poll_link_watchdog()` emits `{"warn":"no_link","age_ms":N}` after 5000 ms (`STALE_WARN_MS`) of silence. `setup()` opens `Serial` and `Serial1` both at 115200 baud (`USB_BAUD`/`LINK_BAUD`); `loop()` calls the three poll functions each iteration.
- **platformio.ini** — PlatformIO project file: `env:uno_r4_minima`, `platform=renesas-ra`, `board=uno_r4_minima`, `framework=arduino`, `monitor_speed=115200`.

**Responsibility:** decode the PSoC6's V3 presence frames off Serial1 and re-expose them as JSON on USB Serial, with a debug passthrough mode and link-health reporting.
**Relationships:** imports from `<Arduino.h>` only (no shared headers with `src/`); depends conceptually on the wire format defined in `src/radar_protocol.h`, reimplemented independently in this file. Independent PlatformIO build, excluded from the ModusToolbox build via `.cyignore`.
